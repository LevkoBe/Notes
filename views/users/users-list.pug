extends ../layout

block content
    .container
        .row
            .col-md-6
            .col-md-6
                h2 Users List
                ul
                    each user in users
                        li
                            a(href=`/users/${user.id}`)= user.username
